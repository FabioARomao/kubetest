apiVersion: v1
kind: PersistentVolume
metadata:
  name: prometheus-storage
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/dataprom"
